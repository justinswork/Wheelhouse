<Window x:Class="Wheelhouse.UI.Views.CreateTagDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Tag" Height="200" Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BrushBackground}"
        ShowInTaskbar="False">
    <StackPanel Margin="12">
        <TextBlock Text="Tag name:" FontSize="12" Foreground="{DynamicResource BrushOnSurface}" Margin="0,0,0,4" />
        <TextBox x:Name="TagNameBox" FontSize="12" Padding="4,3" Margin="0,0,0,8"
                 TextChanged="OnNameChanged" />
        <TextBlock Text="Message (leave blank for lightweight tag):" FontSize="12"
                   Foreground="{DynamicResource BrushOnSurface}" Margin="0,0,0,4" />
        <TextBox x:Name="MessageBox" FontSize="12" Padding="4,3" Margin="0,0,0,12" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CreateButton" Content="Create" IsDefault="True" Width="72" Margin="0,0,8,0"
                    Click="OnCreate" IsEnabled="False" />
            <Button Content="Cancel" IsCancel="True" Width="72" />
        </StackPanel>
    </StackPanel>
</Window>
