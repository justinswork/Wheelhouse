<UserControl x:Class="Wheelhouse.UI.Views.TerminalPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Wheelhouse.UI.ViewModels"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             d:DataContext="{d:DesignInstance vm:TerminalPaneViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <DockPanel Background="#0D1117">

        <!-- Tab strip -->
        <Border DockPanel.Dock="Top" Background="#161B22"
                BorderBrush="#30363D" BorderThickness="0,0,0,1">
            <DockPanel>
                <!-- New tab button -->
                <Button DockPanel.Dock="Right" Content="+"
                        Command="{Binding AddTabCommand}"
                        Width="28" Background="Transparent" BorderThickness="0"
                        Foreground="#7D8590" FontSize="16" Cursor="Hand"
                        ToolTip="New terminal tab" />

                <!-- Tabs -->
                <ItemsControl ItemsSource="{Binding Tabs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:TerminalTabViewModel}">
                            <Border x:Name="TabBorder"
                                    Padding="10,4" Cursor="Hand"
                                    BorderBrush="#30363D" BorderThickness="0,0,1,0"
                                    Background="Transparent"
                                    MouseLeftButtonDown="OnTabClick">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Header}" FontSize="12"
                                               Foreground="#7D8590" VerticalAlignment="Center" />
                                    <Button Content="×" Margin="6,0,0,0"
                                            Command="{Binding CloseCommand}"
                                            FontSize="11" Padding="0"
                                            Background="Transparent" BorderThickness="0"
                                            Foreground="#7D8590" Cursor="Hand"
                                            VerticalAlignment="Center" />
                                </StackPanel>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                <Setter Property="Background" Value="#0D1117" />
                                                <Setter Property="BorderThickness" Value="0,2,1,0" />
                                                <Setter Property="BorderBrush" Value="#58A6FF" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DockPanel>
        </Border>

        <!-- WebView2 host -->
        <wv2:WebView2 x:Name="WebView" />
    </DockPanel>
</UserControl>
